---
name: external-intel
description: Gather current, source-backed security and implementation context for Argos nodes, including expected behavior, upstream code, advisories, fixes, versions, and duplicate risk. Use before deep investment and whenever old knowledge may have changed.
---

# External Intel

Use external research to learn what the local map does not establish.

Record only durable target or dependency knowledge in Argos. Keep search logs, task state, tool issues, and source-collection progress outside the graph.

Search primary sources for expected behavior, version scope, configuration defaults, upstream implementation, advisories, fixes, and prior reports. Compare the exact root cause and attacker path, not only keywords or a similar impact label.

Update the canonical `intel`, `dependency`, component, or finding node. Link it to the local item it changes. Include the source, publication or commit date, affected version, and the narrow fact it supports in natural prose.

Treat old intel as a lead. Recheck it when the target or dependency changed. A public fix for a related path does not prove the current path fixed, and an issue or TODO does not establish that the security impact is known.

Use intel before deep work to reduce duplicates, then return to it when a new symbol, behavior, or dependency gives a better query. Do not research only to satisfy a gate.
