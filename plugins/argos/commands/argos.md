---
description: Build or continue an Argos target knowledge map.
argument-hint: [target or research goal]
---

Use the `argos` skill as the operating guide. Initialize Argos at the workspace root if needed, recover the smallest relevant linked subgraph, and continue the user's goal without creating duplicate notes. Treat relation suggestions as leads that require review.

User context: $ARGUMENTS
